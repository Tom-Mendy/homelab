# Agentic Workloads

one long live agentic (use VM in Kubernetes via KubeVirt in container don't work with):

- hermes

additionally, enhance agentics features via:

- hindsight

spawn agentic container to replace "Cloud" in homelab Kubernetes cluster
interface with T3Code / Herdr so it offload my personal computer using git worktrees
