infisical:
  replicaCount: 1
  kubeSecretRef: infisical-secrets
  image:
    repository: infisical/infisical
    tag: v0.164.1@sha256:d3ff9f82faa786fddf83ce015331c3725bac56db22f6444f48b3b02ff3c772f9
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
  extraEnv:
    - name: ALLOW_INTERNAL_IP_CONNECTIONS
      value: "true"

ingress:
  enabled: true
  hostName: infisical.home.tom-mendy.com
  ingressClassName: traefik
  nginx:
    enabled: false
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
  tls:
    - hosts:
        - infisical.home.tom-mendy.com

postgresql:
  enabled: false
  useExistingPostgresSecret:
    enabled: true
    existingConnectionStringSecret:
      name: infisical-postgres-app
      key: uri

redis:
  enabled: true
  architecture: standalone
  usePassword: false
  auth:
    enabled: false
    password: ""
  master:
    persistence:
      enabled: true
      storageClass: nfs-k8s
      size: 5Gi
