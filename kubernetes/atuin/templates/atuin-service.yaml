apiVersion: v1
kind: Service
metadata:
  name: atuin
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: atuin
  ports:
    - name: http
      port: {{ .Values.atuin.port }}
      targetPort: {{ .Values.atuin.port }}