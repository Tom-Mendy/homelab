namespace: searxng
createNamespace: true

replicaCount: 1

image:
  repository: searxng/searxng
  tag: latest
  pullPolicy: IfNotPresent

deploymentAnnotations:
  keel.sh/policy: all

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  host: searxng.home.tom-mendy.com
  className: ""
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web, websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt

env:
  instanceName: SearXNG
  baseUrl: https://searxng.home.tom-mendy.com/
  autocomplete: duckduckgo

secret:
  name: searxng-secret
  serverSecretKey: "FdfgPLztckqMFZleMTjJL6THzgpRIIpzRWTgvkNQ+Vak4GYjClRm/5nNkU4fWsYp"

vpn:
  enabled: true
  createSecret: true
  secretName: searxng-vpn-secret
  serviceProvider: protonvpn
  type: wireguard
  firewall: "on"
  serverCountries: "France"
  credentials:
    wireguardPrivateKey: "4CRPmvGD799Plg87IfW1gFNe6+3FVzpULVJ/EqodX0g="
