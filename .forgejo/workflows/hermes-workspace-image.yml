name: hermes-workspace-image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - kubernetes/coder/workspace-images/hermes/**
      - .forgejo/workflows/hermes-workspace-image.yml

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      IMAGE: forgejo.tom-mendy.com/tom-mendy/hermes-workspace:v2026.7.1-apt1
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Log in to Forgejo registry
        run: echo "$REGISTRY_TOKEN" | docker login forgejo.tom-mendy.com --username "$REGISTRY_USERNAME" --password-stdin
        env:
          REGISTRY_USERNAME: ${{ forgejo.repository_owner }}
          REGISTRY_TOKEN: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build and push image
        run: |
          docker build --pull --tag "$IMAGE" kubernetes/coder/workspace-images/hermes
          for attempt in 1 2 3; do
            docker push "$IMAGE" && exit 0
            sleep 15
          done
          exit 1
